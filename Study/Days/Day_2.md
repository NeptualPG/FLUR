# Day #2

Due to limited time, I didn’t build a lot today, but I learned quite a bit.

## What worked:
* Structured the project into modules
* Created validator modules
* Started using utils (errorHandler)
* Resolved MongoDB conflicts
* Finished UPDATE and DELETE endpoints
* Basic testing with curl

## What I learned:
* How to validate input (validators)
* How error handlers reduce repeated code
* Better project structure (modularization)
* Arrow functions (lambda functions)