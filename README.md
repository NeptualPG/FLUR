 # This is my first individual first project

 * How can I by my self make a set up 
 * Docker bases
 * Use git
 * Ci-pipeline
 * java script
 * Api rest (Express js)
 * Frontend (React)
 

TO run the aplication 
node index.js

Start the project: 

Server : node index.js

remove: sudo rm -f /tmp/mongodb-27017.sock
Mongo Manually : mongod --dbpath ~/data/db --unixSocketPrefix ~/data

curl example: 

        curl -X PUT http://localhost:3000/api/user/ID \
        -H "Content-Type: application/json" \
        -d '{"name":"Updated","age":30}'

# Progress

this code just has user api to start and I'm going to start to put more content
but while I'm going continue with the frontend to go implment at time